# Exclui o .env do build context — segredos nunca entram nas camadas da imagem (T-02-03)
.env
.git
**/bin
**/obj
.planning
*.md
.playwright-mcp

# Exclui o SQLite local (app.db) — PII/hashes de senha do dev não entram no build context (T-02-03)
**/app.db
**/*.db