---
phase: 02-docker-docs
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .github/workflows/ci-cd.yml
  - ARCHITECTURE.md
  - README.md
autonomous: true
requirements: [extras]
must_haves:
  truths:
    - "ARCHITECTURE.md (PT-BR) documenta camadas, fluxo de autenticação e deploy compose com diagramas Mermaid (D-12/D-13)"
    - "Pipeline CI roda build + dotnet test em push para main e pull requests (D-10/D-11)"
    - "Pipeline NÃO contém SonarQube (D-10)"
    - "README não menciona SonarQube e documenta o modo Docker (D-10)"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Entregável do teste da vaga (D-14): diagramas + justificativas por nível"
      contains: "```mermaid"
    - path: ".github/workflows/ci-cd.yml"
      provides: "Pipeline CI build+test (D-10/D-11)"
      contains: "pull_request"
    - path: "README.md"
      provides: "Docs atualizadas — seção Docker + remoção do SonarQube"
      contains: "docker compose up --build"
  key_links:
    - from: "ARCHITECTURE.md"
      to: "docker-compose.yml"
      via: "diagrama de deploy reflete serviços/portas/volume reais do compose (db postgres:16, api 5290:8080, postgres_data)"
      pattern: "postgres_data"
    - from: ".github/workflows/ci-cd.yml"
      to: "solution/DotnetUserManagementApi.sln"
      via: "dotnet restore/build/test apontando para o sln"
      pattern: "DotnetUserManagementApi.sln"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "instruções `cp .env.example .env` + `docker compose up --build`"
      pattern: "docker compose up --build"
---

<objective>
Entregar a documentação de arquitetura (entregável do teste da vaga) e o pipeline CI/CD do repositório.

Purpose: Cumprir os success criteria 2 e 3 do ROADMAP — `ARCHITECTURE.md` documentando camadas, fluxo de autenticação e decisões; `.github/workflows/ci-cd.yml` pronto. A documentação descreve a arquitetura containerizada REAL criada no plan 02-01 (serviços, portas, volume, env vars).

Output: `ARCHITECTURE.md` (PT-BR, Mermaid), `.github/workflows/ci-cd.yml` (build+test, sem SonarQube), `README.md` atualizado.
</objective>

## Artifacts this phase produces

- **Arquivo novo:** `ARCHITECTURE.md` (documento PT-BR com ≥3 diagramas Mermaid: arquitetura em camadas, fluxo de autenticação, deploy compose; justificativas por nível e tabela de decisões)
- **Arquivo novo:** `.github/workflows/ci-cd.yml` (workflow `CI` com job `build-test`: checkout@v4, setup-dotnet@v4 `8.0.x`, `dotnet restore`, `dotnet build -c Release --no-restore`, `dotnet test -c Release --no-build` — todos contra `solution/DotnetUserManagementApi.sln`; triggers `push: [main]` + `pull_request`)
- **Arquivo modificado:** `README.md` (seção "Rodar com Docker" + remoção de "análise estática (SonarQube)" do "Próximos passos" + entrada nova na tabela de decisões)
- **Nenhum símbolo de código novo** — fase de documentação/CI apenas

<execution_context>
@/home/silvi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/silvi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-docker-docs-extra-p-s-mvp/02-CONTEXT.md
@.planning/phases/02-docker-docs-extra-p-s-mvp/02-PATTERNS.md
@README.md
@Dockerfile
@docker-compose.yml
@.env.example
@src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs
@src/DotnetUserManagementApi.Api/Program.cs
@tests/DotnetUserManagementApi.Tests/DotnetUserManagementApi.Tests.csproj
@solution/DotnetUserManagementApi.sln
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workflow CI/CD (build + dotnet test) e atualização do README</name>
  <files>
    .github/workflows/ci-cd.yml
    README.md
  </files>
  <read_first>
    - tests/DotnetUserManagementApi.Tests/DotnetUserManagementApi.Tests.csproj (linhas 8-9: `IsTestProject=true` — o `dotnet test` do sln roda este projeto)
    - solution/DotnetUserManagementApi.sln (5 projetos: Domain, Application, Infrastructure, Api, Tests)
    - README.md (linhas 50-53 comandos de teste; linhas 71-85 tabela de decisões + "Próximos passos" com menção ao SonarQube na linha 84 — alvo da remoção D-10)
    - .planning/phases/02-docker-docs-extra-p-s-mvp/02-PATTERNS.md (seção `.github/workflows/ci-cd.yml`)
    - .planning/phases/02-docker-docs-extra-p-s-mvp/02-CONTEXT.md (D-10: sem SonarQube; D-11: push main + pull requests)
  </read_first>
  <action>
    Crie o pipeline CI e atualize o README (PT-BR, D-12):

    1. **`.github/workflows/ci-cd.yml`** — workflow com:
       - `name: CI`
       - Triggers (D-11): `on.push.branches: [main]` e `on.pull_request:` (sem filtros de path — repo inteiro)
       - Um único job `build-test` em `runs-on: ubuntu-latest` (seguro: testes usam SQLite in-memory — `TestWebAppFactory`; o sln usa caminhos que `dotnet` normaliza no Linux)
       - Steps, nesta ordem:
         1. `actions/checkout@v4`
         2. `actions/setup-dotnet@v4` com `dotnet-version: '8.0.x'` (alinhado ao `TargetFramework net8.0` e pré-requisito do README)
         3. `dotnet restore solution/DotnetUserManagementApi.sln`
         4. `dotnet build solution/DotnetUserManagementApi.sln --configuration Release --no-restore`
         5. `dotnet test solution/DotnetUserManagementApi.sln --configuration Release --no-build`
       - NENHUM passo de SonarQube (D-10 — cancelado pelo usuário), nenhum secret, nenhum passo de deploy (build+test apenas). Nomes de step em PT-BR (`Restaurar pacotes`, `Build`, `Testes` ou equivalentes) ou EN — escolha um e mantenha consistente.

    2. **`README.md`** — duas mudanças (mantendo o estilo PT-BR existente):
       - **Remover o SonarQube (D-10):** na seção "Próximos passos (fora do MVP)" (linhas 81-85), remova a menção "análise estática (SonarQube)" da linha 84. Substitua a seção inteira por uma seção "Rodar com Docker (PostgreSQL)" com `cp .env.example .env` + `docker compose up --build` + nota de que a API sobe em `http://localhost:5290` com PostgreSQL 16 em container (volume `postgres_data`), e uma linha apontando que Dockerfile/compose, pipeline CI (build + testes) e `ARCHITECTURE.md` foram entregues nesta fase.
       - **Tabela de decisões (linhas 71-79):** adicione uma linha `| EF Core dual-provider (SQLite local / PostgreSQL Docker) | Zero configuração local e Postgres prod-like via chave ConnectionStrings:Database |` e atualize a linha "EF Core + SQLite" se necessário para refletir o dual-provider (D-01).
       - Não remova as demais seções; o resto do README permanece intacto.
  </action>
  <verify>
    <automated>grep -c "SonarQube" .github/workflows/ci-cd.yml README.md; grep -c "dotnet test" .github/workflows/ci-cd.yml; grep -n "pull_request" .github/workflows/ci-cd.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "main" .github/workflows/ci-cd.yml` retorna a linha do trigger `push` com `branches: [main]`
    - `grep -n "pull_request" .github/workflows/ci-cd.yml` retorna a linha do trigger de PR (D-11)
    - `grep -c "SonarQube" .github/workflows/ci-cd.yml` retorna 0 (D-10)
    - `grep -c "SonarQube" README.md` retorna 0 (D-10)
    - `grep -n "dotnet test" .github/workflows/ci-cd.yml` retorna o step de teste com `--no-build`
    - `grep -n "actions/setup-dotnet@v4" .github/workflows/ci-cd.yml` retorna o step com `dotnet-version: '8.0.x'`
    - `grep -n "docker compose up --build" README.md` retorna a instrução na nova seção
    - `grep -n "postgres_data" README.md` retorna a menção ao volume na seção Docker
    - Reprodução local dos comandos do CI: `dotnet restore solution/DotnetUserManagementApi.sln && dotnet build solution/DotnetUserManagementApi.sln --configuration Release --no-restore && dotnet test solution/DotnetUserManagementApi.sln --configuration Release --no-build` sai com código 0
    - `git status` mostra apenas `.github/workflows/ci-cd.yml` e `README.md` como modificados nesta task (nada além)
  </acceptance_criteria>
  <done>
    Pipeline CI com build + teste (sem SonarQube) disparando em push main e PRs; README sem menção a SonarQube, com seção Docker e decisão dual-provider documentada.
  </done>
</task>

<task type="auto">
  <name>Task 2: ARCHITECTURE.md — entregável do desafio (camadas, fluxo de auth, deploy compose)</name>
  <files>
    ARCHITECTURE.md
  </files>
  <read_first>
    - README.md (analog de estilo: PT-BR, seções `##`, tabelas markdown de decisões, fenced blocks bash — linhas 71-85)
    - Dockerfile e docker-compose.yml (criados no plan 02-01 — a doc descreve a topologia REAL: serviços `db`/`api`, porta 5290:8080, volume `postgres_data`, healthcheck `pg_isready`, env vars)
    - .env.example (nomes das variáveis a documentar — nunca valores)
    - src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs (registro dual-provider — D-01/D-03)
    - src/DotnetUserManagementApi.Api/Program.cs (fail-fast Jwt:Key D-08; init de banco D-02/D-07; guard Swagger D-05)
    - .planning/phases/02-docker-docs-extra-p-s-mvp/02-CONTEXT.md (D-12..D-14 e `<specifics>` — critérios VERBATIM do teste da vaga)
    - .planning/phases/02-docker-docs-extra-p-s-mvp/02-PATTERNS.md (seção `ARCHITECTURE.md`)
  </read_first>
  <action>
    Escreva `ARCHITECTURE.md` na raiz do repo, em PT-BR (D-12), como entregável do teste da vaga (D-14). Estrutura e conteúdo obrigatórios:

    1. **Abertura:** título + visão geral de 1-2 linhas no estilo da linha 3 do README (o que é a API, stack, propósito do documento).

    2. **Arquitetura em camadas** — diagrama Mermaid `flowchart` mostrando as 4 camadas Clean Architecture (Api → Application → Domain; Infrastructure transversal; Tests) e suas responsabilidades. Em seguida, **justificativas por camada** (D-14: "justificativas claras para as tecnologias e padrões escolhidos em cada nível"): Api (controllers + middleware `ExceptionHandlingMiddleware` + Swagger dev-only), Application (use cases, DTOs, contratos), Domain (entidade `User` + regras como `Email` e `MaxNameLength`), Infrastructure (EF Core, BCrypt work factor 12, emissão JWT HS256).

    3. **Fluxo de autenticação** — diagrama Mermaid `sequenceDiagram` cobrindo: `POST /api/auth/register` (hash BCrypt → persistência → 201), `POST /api/auth/login` (validação BCrypt → `JwtTokenService` emite JWT HS256 com Issuer/Audience/ExpirationMinutes → 200 com token), `GET /api/users` (middleware JwtBearer valida assinatura/issuer/audience/lifetime → 200 autorizado, 401 sem/inválido). Justificativas: JWT HS256 stateless; BCrypt com salt; chave nunca versionada.

    4. **Persistência dual-provider** — chave `ConnectionStrings:Database` (`"Sqlite"` | `"Postgres"`): SQLite local com `EnsureCreated` (zero dependência) vs PostgreSQL Docker com migrações EF Core + retry limitado no startup (D-01..D-04, D-07). Justificativa da escolha e tabela comparando os dois modos.

    5. **Deploy com Docker Compose** — diagrama Mermaid `graph` (ou `flowchart`) com: container `api` (Dockerfile multi-stage: build `sdk:8.0` → runtime `aspnet:8.0-alpine`, porta host 5290 → 8080), container `db` (`postgres:16`), volume nomeado `postgres_data`, healthcheck `pg_isready` + `depends_on: service_healthy`. Tabela de variáveis de ambiente do compose (nomes `ASPNETCORE_ENVIRONMENT`, `ConnectionStrings__Database`, `ConnectionStrings__Default`, `Jwt__Key` + `.env` com `JWT__KEY`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`) com nota de que `.env.example` é versionado com placeholders e o `.env` real nunca é (D-09).

    6. **Segurança** — resumo: `JWT__KEY` obrigatória com fail-fast fora de Development (D-08), senhas com BCrypt (QUAL-01), secrets só em `.env` gitignored, Postgres sem porta exposta ao host.

    7. **CI/CD** — parágrafo descrevendo `.github/workflows/ci-cd.yml`: build + `dotnet test` em push main e pull requests, sem SonarQube (D-10/D-11). Exemplo conceitual do YAML apenas se julgar necessário ilustrar (permitido por D-14).

    8. **Decisões técnicas** — tabela markdown no estilo README (linhas 71-79) consolidando: Clean Architecture, JWT HS256, BCrypt work factor 12, EF Core dual-provider, Docker multi-stage + compose prod-like, `InvariantGlobalization=true` (imagem sem libicu).

    9. **Critérios de entrega (D-14):** seção final mapeando os 3 critérios verbatim do `<specifics>` do CONTEXT.md para as seções do documento (diagramas ✓, justificativas por nível ✓, exemplos conceituais onde necessário ✓).

    Regras: ≥3 blocos ```` ```mermaid ```` (D-13: arquitetura, fluxo de auth, deploy — um por diagrama); todo o texto em PT-BR; sem valores reais de segredo (apenas nomes de variáveis); sem fenced blocks de código além dos diagramas e, no máximo, o exemplo conceitual de YAML do CI.
  </action>
  <verify>
    <automated>grep -c '```mermaid' ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '```mermaid' ARCHITECTURE.md` retorna ≥ 3 (D-13: arquitetura, fluxo de auth, deploy compose)
    - `grep -n "sequenceDiagram" ARCHITECTURE.md` retorna 1+ ocorrências (fluxo de autenticação)
    - `grep -n "postgres_data" ARCHITECTURE.md` retorna a menção ao volume no diagrama/seção de deploy
    - `grep -n "JWT__KEY" ARCHITECTURE.md` retorna a seção de segurança/variáveis (nome da variável, sem valor real)
    - `grep -n "ConnectionStrings__Database" ARCHITECTURE.md` retorna a menção ao dual-provider
    - `grep -n "SonarQube" ARCHITECTURE.md` NÃO retorna ocorrência como recurso planejado (se mencionado, apenas como "não incluído por decisão" — D-10)
    - `grep -c "SonarQube" ARCHITECTURE.md` é 0 ou a única ocorrência é a negação explícita (D-10)
    - `wc -l ARCHITECTURE.md` retorna ≥ 120 linhas (documento substancial, entregável)
    - Nenhuma linha do arquivo contém valores reais de segredo (hex de 64 bytes ou senha real) — conferir visualmente as seções de env vars
  </acceptance_criteria>
  <done>
    ARCHITECTURE.md entregue em PT-BR com 3 diagramas Mermaid (camadas, auth flow, deploy compose), justificativas por nível, seções de persistência/segurança/CI e mapeamento dos critérios do desafio.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → GitHub Actions | código do PR/push executado em runner ubuntu-latest |
| docs → reviewer do desafio | ARCHITECTURE.md lido por avaliador externo (entregável) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Supply chain das GitHub Actions | mitigate | Actions pinadas por major version (`checkout@v4`, `setup-dotnet@v4`); workflow sem secrets e sem passo de deploy (build+test apenas — D-10), logo não há material secreto a vazar |
| T-02-08 | Information Disclosure | Segredos vazando em ARCHITECTURE.md / README.md | mitigate | Docs citam apenas NOMES de variáveis (`JWT__KEY`, `POSTGRES_PASSWORD`) e placeholders do `.env.example`; instrução explícita de que `.env` nunca é versionado; nenhum valor real em fenced blocks |
| T-02-09 | Elevation of Privilege | Execução de código não confiável em PRs (trigger `pull_request`) | accept | Job roda apenas restore/build/test do código do PR; sem secrets no ambiente do job; sem passos de publish/upload — risco residual baixo e aceito (build+test não manipulam segredos) |

## Security Conventions (this plan)

- Docs e nomes de step em PT-BR (D-12)
- Workflow sem variáveis secretas do repositório (nenhum `${{ secrets.* }}` necessário para build+test)
- ARCHITECTURE.md documenta o fail-fast de `JWT__KEY` como mitigação (coerência com o plan 02-01, T-02-01)
</threat_model>

<verification>
## Phase Checks (success criteria 2 e 3 do ROADMAP)

1. `ARCHITECTURE.md` existe na raiz, em PT-BR, com ≥3 diagramas Mermaid cobrindo camadas, fluxo de autenticação e deploy compose, e justificativas por nível (D-13/D-14)
2. `.github/workflows/ci-cd.yml` pronto: triggers push main + pull_request, build + `dotnet test` apenas, sem SonarQube (D-10/D-11)
3. Comandos do CI reproduzidos localmente com exit 0 (`dotnet restore` → `build -c Release --no-restore` → `test -c Release --no-build` contra o sln)
4. `README.md` sem "SonarQube" e com a seção "Rodar com Docker" (D-10)
</verification>

<success_criteria>
- `ARCHITECTURE.md` documenta camadas, fluxo de autenticação e decisões com diagramas Mermaid (ROADMAP SC-2)
- `.github/workflows/ci-cd.yml` pronto no repositório (ROADMAP SC-3) — build + `dotnet test` em push main e PRs, sem SonarQube
- README atualizado e consistente com as decisões da fase (D-10, D-12)
</success_criteria>

<output>
Create `.planning/phases/02-docker-docs-extra-p-s-mvp/02-02-SUMMARY.md` when done
</output>