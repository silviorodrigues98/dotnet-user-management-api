---
phase: 02-docker-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DotnetUserManagementApi.Infrastructure/DotnetUserManagementApi.Infrastructure.csproj
  - src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs
  - src/DotnetUserManagementApi.Api/appsettings.json
  - src/DotnetUserManagementApi.Api/Program.cs
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - .env.example
  - .gitignore
  - src/DotnetUserManagementApi.Infrastructure/Persistence/AppDbContext.cs
autonomous: true
requirements: [extras]
user_setup:
  - service: docker
    why: "Executar `docker compose up --build` (success criterion 1 do ROADMAP)"
    note: "Docker + docker compose instalados e daemon ativo. Se ausente, reportar como blocker no SUMMARY."
must_haves:
  truths:
    - "`docker compose up --build` sobe API + PostgreSQL com um comando"
    - "API containerizada usa PostgreSQL 16 com volume nomeado e aplica migrações EF Core no startup"
    - "Run local continua SQLite zero-dependência (dotnet run funciona; dotnet test 12 verdes)"
    - "API falha rápido (fail-fast) ao iniciar em produção sem JWT__KEY"
    - "Secrets ficam fora do versionamento (.env gitignored; .env.example apenas placeholders)"
  artifacts:
    - path: "Dockerfile"
      provides: "Build multi-stage (sdk → runtime) da API"
      contains: "FROM mcr.microsoft.com/dotnet/sdk:8.0"
    - path: "docker-compose.yml"
      provides: "Orquestração prod-like API + PostgreSQL 16 com healthcheck e volume"
      contains: "postgres:16"
    - path: "src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs"
      provides: "Seleção de provider por ConnectionStrings:Database (D-01/D-03)"
      exports: ["AddInfrastructure"]
    - path: "src/DotnetUserManagementApi.Api/Program.cs"
      provides: "Fail-fast Jwt:Key (D-08) + init de banco por provider com retry (D-02/D-07)"
      contains: "EnsureCreated"
    - path: ".env.example"
      provides: "Template de secrets com placeholders (D-09)"
      contains: "JWT__KEY="
  key_links:
    - from: "docker-compose.yml"
      to: "src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs"
      via: "ConnectionStrings__Database=Postgres → branch UseNpgsql"
      pattern: "UseNpgsql"
    - from: "src/DotnetUserManagementApi.Api/Program.cs"
      to: "ConnectionStrings:Database"
      via: "branch no startup (Migrate vs EnsureCreated)"
      pattern: "EnsureCreated"
    - from: ".env"
      to: "docker-compose.yml"
      via: "interpolação ${JWT__KEY:?} e ${POSTGRES_PASSWORD:?}"
      pattern: "\\$\\{JWT__KEY"
    - from: ".gitignore"
      to: ".env"
      via: "exclusão do arquivo real de secrets"
      pattern: "^\\.env$"
---

<objective>
Containerizar a API com Dockerfile multi-stage e docker-compose prod-like com PostgreSQL 16, mantendo o run local SQLite zero-dependência.

Purpose: Entregar o success criterion 1 do ROADMAP — `docker compose up --build` sobe API + PostgreSQL com um comando — sem quebrar o fluxo local nem os 12 testes verdes da Phase 1.

Output: `Dockerfile`, `docker-compose.yml`, `.env.example`, `.dockerignore`, e as modificações em `Infrastructure.csproj`, `DependencyInjection.cs`, `Program.cs`, `appsettings.json`, `.gitignore` (e `AppDbContext.cs` apenas na contingência de migração).
</objective>

## Artifacts this phase produces

- **Arquivos novos:** `Dockerfile`, `docker-compose.yml`, `.env.example`, `.dockerignore`
- **Serviços docker compose:** `db` (imagem `postgres:16`, volume `postgres_data`, healthcheck `pg_isready`) e `api` (build do Dockerfile, porta host `5290` → container `8080`)
- **Config keys novas:** `ConnectionStrings:Database` (`"Sqlite"` | `"Postgres"`), lida em `DependencyInjection.cs` (registro do DbContext) e `Program.cs` (init do banco)
- **Env vars do compose (double-underscore):** `ASPNETCORE_ENVIRONMENT=Production`, `ASPNETCORE_URLS=http://+:8080`, `ConnectionStrings__Database=Postgres`, `ConnectionStrings__Default=Host=db;Port=5432;...`, `Jwt__Key=${JWT__KEY:?}`
- **Env vars do `.env`:** `JWT__KEY`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`
- **Package novo:** `Npgsql.EntityFrameworkCore.PostgreSQL` (8.0.x pinada) no `Infrastructure.csproj`
- **Método/função nova:** branch de provider dentro de `AddInfrastructure` (mantém a assinatura `AddInfrastructure(IServiceCollection, IConfiguration)`)

<execution_context>
@/home/silvi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/silvi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-docker-docs-extra-p-s-mvp/02-CONTEXT.md
@.planning/phases/02-docker-docs-extra-p-s-mvp/02-PATTERNS.md
@src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs
@src/DotnetUserManagementApi.Api/Program.cs
@src/DotnetUserManagementApi.Api/appsettings.json
@src/DotnetUserManagementApi.Infrastructure/DotnetUserManagementApi.Infrastructure.csproj
@.gitignore
@src/DotnetUserManagementApi.Infrastructure/Persistence/AppDbContext.cs
@src/DotnetUserManagementApi.Infrastructure/Persistence/Migrations/20260819220815_InitialCreate.cs
@tests/DotnetUserManagementApi.Tests/TestWebAppFactory.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-provider (SQLite local | PostgreSQL) no registro e no startup</name>
  <files>
    src/DotnetUserManagementApi.Infrastructure/DotnetUserManagementApi.Infrastructure.csproj
    src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs
    src/DotnetUserManagementApi.Api/appsettings.json
    src/DotnetUserManagementApi.Api/Program.cs
  </files>
  <read_first>
    - src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs (linha 15-16: chamada fixa `UseSqlite` — ponto de branch do D-01/D-03)
    - src/DotnetUserManagementApi.Api/Program.cs (linhas 18-35: bloco Jwt; linhas 103-107: bloco `Migrate()` — pontos de mudança D-08/D-02/D-07)
    - src/DotnetUserManagementApi.Api/appsettings.json (bloco ConnectionStrings — adicionar a chave `Database`)
    - src/DotnetUserManagementApi.Infrastructure/DotnetUserManagementApi.Infrastructure.csproj (linhas 7-11: formato pinado dos PackageReferences)
    - tests/DotnetUserManagementApi.Tests/TestWebAppFactory.cs (linhas 21-27: sobrescreve com `UseSqlite` — o branch padrão DEVE continuar SQLite para os testes não quebrarem)
    - src/DotnetUserManagementApi.Infrastructure/Security/JwtOptions.cs (linha 5: `JwtOptions.SectionName = "Jwt"`)
  </read_first>
  <action>
    Implemente a seleção dual-provider guiada pela chave explícita `ConnectionStrings:Database` (D-01, D-03, D-04):

    1. **`Infrastructure.csproj`** — adicione `<PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="8.0.21" />` no ItemGroup existente (linhas 7-11), no mesmo formato das demais (versão pinada, sem wildcard). Se `8.0.21` não existir no NuGet, use a versão `8.0.x` mais alta disponível (confira com `dotnet package search Npgsql.EntityFrameworkCore.PostgreSQL --exact-match`); o requisito é alinhamento com a linha EF Core 8.0.x já usada no repo (8.0.21 em Sqlite/JwtBearer/Design).

    2. **`DependencyInjection.cs`** — substitua a chamada fixa `options.UseSqlite(...)` (linha 15-16) por um branch baseado em `configuration.GetConnectionString("Database")`:
       - Se o valor for `"Postgres"` (comparação `StringComparison.OrdinalIgnoreCase`) → `options.UseNpgsql(configuration.GetConnectionString("Default"))`. Adicione `using Npgsql.EntityFrameworkCore.PostgreSQL;` no topo do arquivo.
       - Caso contrário (ausente, `"Sqlite"`, qualquer outro valor) → `options.UseSqlite(configuration.GetConnectionString("Default"))`, exatamente a chamada atual (D-04: default local preservado; TestWebAppFactory continua verde).
       - NÃO altere a assinatura `AddInfrastructure(this IServiceCollection, IConfiguration)` nem os três registros seguintes (UserRepository, BcryptPasswordHasher, JwtTokenService).

    3. **`appsettings.json`** — adicione `"Database": "Sqlite"` dentro de `ConnectionStrings` (após `"Default": "Data Source=app.db"`). NÃO adicione `Jwt:Key` (convenção de segurança: chave nunca versionada — README linha 79).

    4. **`Program.cs` — Fail-fast do Jwt:Key (D-08)** — no bloco existente `if (string.IsNullOrWhiteSpace(jwtOptions.Key))` (linhas 21-26):
       - Se `builder.Environment.IsDevelopment()` → mantenha o comportamento atual (chave aleatória de 64 bytes via `RandomNumberGenerator`, write-back `builder.Configuration["Jwt:Key"]`, mensagem `[SECURITY]` PT-BR atual).
       - Senão (qualquer ambiente não-Development, incluindo Production do compose) → `throw new InvalidOperationException("JWT__KEY (Jwt:Key) é obrigatório em produção. Defina a variável de ambiente JWT__KEY antes de iniciar a API.")`.
       - NÃO altere as linhas 28-35 (`Configure<JwtOptions>` + `PostConfigure`) nem a configuração JwtBearer.

    5. **`Program.cs` — Init de banco por provider com retry (D-02, D-07)** — substitua o bloco `using (var scope = ...) { dbContext.Database.Migrate(); }` (linhas 103-107):
       - Leia o provider com `app.Configuration.GetConnectionString("Database")`.
       - Se `"Postgres"` (OrdinalIgnoreCase) → loop com retry limitado em torno de `dbContext.Database.Migrate()`: no máximo 10 tentativas, `Thread.Sleep(TimeSpan.FromSeconds(2))` entre falhas, capturando `NpgsqlException` (adicione `using Npgsql;` no topo), log PT-BR por tentativa (ex.: `[DB] PostgreSQL indisponível — tentativa {n}/10. Aguardando 2s... ({message})`), e `throw;` quando as tentativas esgotarem. Nenhum script wait-for externo (D-07 explícito).
       - Caso contrário (default/SQLite) → `dbContext.Database.EnsureCreated()` (D-02: local sem migrações).
       - Mantenha o padrão `using (var scope = app.Services.CreateScope())`.
  </action>
  <verify>
    <automated>dotnet build solution/DotnetUserManagementApi.sln && dotnet test solution/DotnetUserManagementApi.sln</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build solution/DotnetUserManagementApi.sln` sai com código 0
    - `dotnet test solution/DotnetUserManagementApi.sln` passa com os 12 testes verdes (TestWebAppFactory inalterado)
    - `grep -n "UseNpgsql" src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs` retorna a linha do branch Postgres
    - `grep -n "UseSqlite" src/DotnetUserManagementApi.Infrastructure/DependencyInjection.cs` retorna a linha do branch default
    - `grep -n '"Database"' src/DotnetUserManagementApi.Api/appsettings.json` retorna `"Database": "Sqlite"`
    - `grep -c "Npgsql.EntityFrameworkCore.PostgreSQL" src/DotnetUserManagementApi.Infrastructure/DotnetUserManagementApi.Infrastructure.csproj` retorna 1 (PackageReference com versão pinada, sem `*`)
    - `grep -n "EnsureCreated" src/DotnetUserManagementApi.Api/Program.cs` retorna a linha do branch SQLite
    - `grep -n "InvalidOperationException" src/DotnetUserManagementApi.Api/Program.cs` retorna a linha do fail-fast (mensagem PT-BR citando `JWT__KEY`)
    - `grep -n "IsDevelopment()" src/DotnetUserManagementApi.Api/Program.cs` retorna 2 ocorrências (guard do Swagger linha 86 + guard do fail-fast)
    - Arquivo `Program.cs` NÃO contém o padrão `dbContext.Database.Migrate();` sem estar dentro do branch Postgres (grep: linha do Migrate existe somente dentro do bloco `if` Postgres)
  </acceptance_criteria>
  <done>
    Provider selecionável por `ConnectionStrings:Database` com default SQLite; Npgsql adicionado e pinado; fail-fast de `Jwt:Key` fora de Development; init de banco branchado com retry para Postgres; build e 12 testes verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile multi-stage + docker-compose prod-like + .env.example + .dockerignore</name>
  <files>
    Dockerfile
    .dockerignore
    docker-compose.yml
    .env.example
    .gitignore
  </files>
  <read_first>
    - src/DotnetUserManagementApi.Api/appsettings.json (superfície de config que o compose sobrescreve por env)
    - .gitignore (seções por comentário — adicionar a seção de secrets)
    - .planning/phases/02-docker-docs-extra-p-s-mvp/02-PATTERNS.md (seções `Dockerfile`, `docker-compose.yml`, `.env.example`, `.gitignore`)
    - src/DotnetUserManagementApi.Api/DotnetUserManagementApi.Api.csproj (linha 21: `InvariantGlobalization=true` → runtime sem libicu)
    - solution/DotnetUserManagementApi.sln (5 projetos; caminhos `..\src\...` relativos à pasta solution)
    - src/DotnetUserManagementApi.Api/Program.cs (linhas 92-101: `wwwroot`/favicon servidos — o publish inclui `wwwroot` automaticamente)
  </read_first>
  <action>
    Crie a containerização prod-like (D-05, D-06, D-07, D-09):

    1. **`Dockerfile`** (raiz do repo) — dois estágios:
       - Estágio `build`: `FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build`, `WORKDIR /src`; copie `Directory.Build.props` (raiz), a pasta `solution/` inteira, `src/` inteira e `tests/` inteira preservando o layout relativo (o .sln referencia `..\src\...` a partir de `solution/`); `RUN dotnet restore solution/DotnetUserManagementApi.sln`; depois `RUN dotnet publish src/DotnetUserManagementApi.Api/DotnetUserManagementApi.Api.csproj --configuration Release --output /app/publish --no-restore` (o publish inclui `wwwroot` — não exclua).
       - Estágio `final`: `FROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine` (permitido porque `InvariantGlobalization=true` dispensa libicu — discretion do usuário), `WORKDIR /app`, `COPY --from=build /app/publish .`, `EXPOSE 8080`, `ENTRYPOINT ["dotnet", "DotnetUserManagementApi.Api.dll"]`.
       - NENHUM `ENV`/`ARG` com valor real de segredo no Dockerfile (T-02-03). Comentários em PT-BR.

    2. **`.dockerignore`** (raiz do repo) — exclua do build context: `.git`, `**/bin`, `**/obj`, `.env` (crítico: nunca entra no contexto de build), `.planning`, `*.md`, `.playwright-mcp`.

    3. **`docker-compose.yml`** (raiz do repo) — dois serviços + volume nomeado, sem chave `version:` (deprecated):
       - Serviço `db`: `image: postgres:16` (D-06); `environment`: `POSTGRES_USER: ${POSTGRES_USER:-app}`, `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?}` (fail-fast do compose), `POSTGRES_DB: ${POSTGRES_DB:-dotnet_user_management}`; `volumes: - postgres_data:/var/lib/postgresql/data` (D-06, volume nomeado — não use bind mount); `healthcheck`: `test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB:-dotnet_user_management}"]`, `interval: 5s`, `timeout: 5s`, `retries: 10`, `start_period: 10s` (D-07); `restart: unless-stopped`. SEM mapeamento de portas para o host (rede interna apenas — T-02-04).
       - Serviço `api`: `build: .`; `environment` (padrão double-underscore do dotnet): `ASPNETCORE_ENVIRONMENT: Production` (Swagger off — D-05), `ASPNETCORE_URLS: http://+:8080`, `ConnectionStrings__Database: Postgres` (D-01), `ConnectionStrings__Default: Host=db;Port=5432;Database=${POSTGRES_DB:-dotnet_user_management};Username=${POSTGRES_USER:-app};Password=${POSTGRES_PASSWORD:?}`, `Jwt__Key: ${JWT__KEY:?}` (D-08 fail-fast no compose); `ports: - "5290:8080"` (mantém a porta documentada no README — curl local funciona igual); `depends_on: db: condition: service_healthy` (D-07); `restart: unless-stopped`. NÃO use `ASPNETCORE_ENVIRONMENT=Development` (D-05).
       - `volumes: postgres_data:` no nível raiz.
       - Comentários em PT-BR.

    4. **`.env.example`** (raiz do repo) — versionado com placeholders (D-09), chaves: `JWT__KEY=changeme-troque-por-uma-chave-aleatoria-de-ao-menos-32-bytes`, `POSTGRES_USER=app`, `POSTGRES_PASSWORD=changeme`, `POSTGRES_DB=dotnet_user_management`. Cabeçalho em PT-BR instruindo `cp .env.example .env` e que o `.env` real nunca é versionado.

    5. **`.gitignore`** — adicione a seção `# Docker compose secrets` com a linha `.env` (D-09), seguindo o padrão de seções com comentário do arquivo.
  </action>
  <verify>
    <automated>docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` sai com código 0 (YAML válido) APÓS `cp .env.example .env`
    - `grep -n "postgres:16" docker-compose.yml` retorna a linha do serviço `db`
    - `grep -n "postgres_data" docker-compose.yml` retorna 2+ ocorrências (mount + declaração do volume)
    - `grep -n "pg_isready" docker-compose.yml` retorna a linha do healthcheck
    - `grep -n "service_healthy" docker-compose.yml` retorna a linha do `depends_on`
    - `grep -n "JWT__KEY" docker-compose.yml` retorna `Jwt__Key: ${JWT__KEY:?}`
    - `grep -n "ASPNETCORE_ENVIRONMENT" docker-compose.yml` retorna `Production` (nunca `Development`)
    - `grep -n "FROM mcr.microsoft.com/dotnet/sdk:8.0" Dockerfile` e `grep -n "FROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine" Dockerfile` retornam 1 ocorrência cada
    - `grep -n "^\.env$" .dockerignore` e `grep -n "^\.env$" .gitignore` retornam 1 ocorrência cada
    - `grep -c "changeme" .env.example` retorna 2 (placeholders em JWT__KEY e POSTGRES_PASSWORD)
    - `grep -n "postgres_data" .gitignore` NÃO retorna ocorrência (volume nomeado, sem bind mount — sem entrada necessária)
  </acceptance_criteria>
  <done>
    Dockerfile multi-stage (sdk 8.0 → aspnet 8.0-alpine), compose com db (postgres:16 + volume + healthcheck) e api (prod-like, porta 5290:8080), `.env.example` com placeholders, `.env` e `.dockerignore` excluindo secrets.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verificação E2E — docker compose sobe, fluxo JWT funcional, persistência e regressão local</name>
  <files>
    .env
    src/DotnetUserManagementApi.Infrastructure/Persistence/AppDbContext.cs
  </files>
  <read_first>
    - Dockerfile, docker-compose.yml, .env.example (criados na Task 2 — o que está sendo executado)
    - src/DotnetUserManagementApi.Api/Program.cs (linhas 21-26 fail-fast e 103-107 init de banco — o que será exercitado)
    - src/DotnetUserManagementApi.Infrastructure/Persistence/Migrations/20260819220815_InitialCreate.cs (todas as colunas com `type: "TEXT"` — base da contingência de migração no Postgres)
    - src/DotnetUserManagementApi.Infrastructure/Persistence/AppDbContext.cs (OnModelCreating — alvo da contingência, apenas se necessário)
    - .planning/phases/02-docker-docs-extra-p-s-mvp/02-CONTEXT.md (D-01..D-09 para conferência do comportamento)
  </read_first>
  <action>
    Valide o ciclo completo (success criterion 1 do ROADMAP) e aplique a contingência de migração se necessário:

    1. **Preflight:** `docker --version && docker compose version` — se Docker ausente, pare e reporte blocker no SUMMARY (user_setup).

    2. **.env real:** `cp .env.example .env`, depois substitua os placeholders por valores reais: `JWT__KEY=$(openssl rand -hex 32)` e `POSTGRES_PASSWORD=$(openssl rand -hex 16)` (edite o `.env` com esses valores gerados — nunca os imprima em logs).

    3. **Regressão local (antes do compose, porta 5290 livre):** `dotnet build solution/DotnetUserManagementApi.sln && dotnet test solution/DotnetUserManagementApi.sln` (12 testes verdes). Smoke local: inicie `dotnet run` em `src/DotnetUserManagementApi.Api` em background com `ASPNETCORE_ENVIRONMENT=Development`, aguarde ~15s, `curl -s -o /dev/null -w "%{http_code}" http://localhost:5290/api/users` deve retornar `401` (EnsureCreated + middleware JWT funcionando no branch default SQLite); encerre o processo.

    4. **Subida do compose:** `docker compose up --build -d`. Aguarde até `docker compose ps` mostrar `db` com status `healthy` (poll até 60s; o retry da Task 1 tolera corrida de readiness — D-07). O container `api` deve estar `running` sem restart em loop (`docker compose ps` mostra `Up`).

    5. **Fluxo funcional (curl contra http://localhost:5290):**
       - `POST /api/auth/register` → 201
       - mesmo e-mail novamente → 201 (anti-enumeração T-01-10 — resposta uniforme)
       - `POST /api/auth/login` → 200 e token JWT no corpo
       - `GET /api/users` sem token → 401
       - `GET /api/users` com `Authorization: Bearer <token>` → 200
       - `GET /` → 200 (página estática + wwwroot servidos do container)

    6. **Persistência (D-06):** registre um usuário, `docker compose restart api`, faça login do mesmo usuário → 200 (dados sobrevivem no volume `postgres_data`).

    7. **Fail-fast (D-08):** `mv .env .env.bak && docker compose config > /dev/null` deve falhar (exit != 0) citando `JWT__KEY` (interpolação `${JWT__KEY:?}`); `mv .env.bak .env` para restaurar e `docker compose config --quiet` volta a passar.

    8. **Contingência de migração (somente se o passo 5 falhar com erro de tipo de coluna, ex.: `column "Id" is of type text but expression is of type uuid`):** a migração existente usa `type: "TEXT"` para TODAS as colunas (incluindo `Guid Id` e `DateTime CreatedAtUtc`). Corrija adicionando `.HasColumnType("TEXT")` às propriedades `Id`, `Name`, `Email`, `PasswordHash` e `CreatedAtUtc` em `AppDbContext.OnModelCreating` (mantendo `HasMaxLength`/`IsRequired`/`HasIndex` atuais) para que o modelo Npgsql envie/leia `text` alinhado à migração. NÃO regenere migrações (a migração única continua válida para os dois providers). Repita os passos 4-6.

    9. **Estado final:** `docker compose down` (sem `-v` — preserva `postgres_data`). Relate no SUMMARY: comandos exatos usados, saídas dos curls (códigos HTTP), resultado da contingência (se aplicada), e confirme que `.env` local não foi commitado.
  </action>
  <verify>
    <automated>docker compose up --build -d && sleep 15 && curl -s -o /dev/null -w "%{http_code}" http://localhost:5290/api/users</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up --build -d` sai com código 0
    - `docker compose ps` mostra `db` `healthy` e `api` `running` (sem restarts em loop)
    - Register → 201; register duplicado → 201 (uniforme, anti-enumeração T-01-10); login → 200 com token; `/api/users` sem token → 401; com token → 200; `/` → 200
    - Após `docker compose restart api`, login do mesmo usuário → 200 (persistência no volume)
    - `docker compose config` com `.env` ausente falha citando `JWT__KEY`; com `.env` restaurado passa (exit 0)
    - `dotnet test solution/DotnetUserManagementApi.sln` passa (12 verdes) — regressão SQLite intacta
    - Se a contingência foi aplicada: `grep -n "HasColumnType" src/DotnetUserManagementApi.Infrastructure/Persistence/AppDbContext.cs` retorna 5 ocorrências (`"TEXT"` em Id, Name, Email, PasswordHash, CreatedAtUtc) e o fluxo do passo 5 volta a passar
    - Nenhum arquivo novo além dos planejados aparece em `git status` (`.env` NÃO pode aparecer)
  </acceptance_criteria>
  <done>
    `docker compose up --build` sobe API + PostgreSQL; register/login/users funcionam contra Postgres; dados persistem em `postgres_data`; fail-fast de `JWT__KEY` comprovado; run local e testes inalterados; `.env` fora do versionamento.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → api | HTTP ingress no host `5290` → container `8080` (única porta exposta) |
| api → db | rede interna do compose; `db` sem portas publicadas no host |
| build context → image | conteúdo do build context vira camadas da imagem (risco de segredo) |
| host filesystem → compose | `.env` do host interpolado nas env vars dos containers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing / Tampering | Chave de assinatura JWT (`JWT__KEY` / `Jwt:Key`) | mitigate | Obrigatória em produção: fail-fast em `Program.cs` (`InvalidOperationException` fora de `IsDevelopment()`, D-08) + `${JWT__KEY:?}` no compose; `.env` gitignored (D-09); `.env.example` só placeholders; chave nunca em `appsettings.json` nem no Dockerfile |
| T-02-02 | Information Disclosure | Credenciais do PostgreSQL (`POSTGRES_PASSWORD`) | mitigate | Valor real apenas em `.env` (gitignored); compose usa `${POSTGRES_PASSWORD:?}`; nunca hardcoded em compose/appsettings/Dockerfile; `.env` excluído do build context via `.dockerignore` |
| T-02-03 | Tampering | Segredos em camadas da imagem Docker | mitigate | `.dockerignore` exclui `.env`, `.git`, `.planning`; Dockerfile sem `ENV`/`ARG` com valores reais; config injetada em runtime via `environment` do compose |
| T-02-04 | Information Disclosure | Porta do PostgreSQL exposta ao host | mitigate | Serviço `db` sem bloco `ports:` — apenas rede interna do compose; única porta publicada é `5290:8080` da API |
| T-02-05 | Tampering | Dependência `Npgsql.EntityFrameworkCore.PostgreSQL` | accept | Pacote NuGet do ecossistema oficial Npgsql, restaurado de nuget.org, versão 8.0.x pinada exatamente (convenção do repo, sem wildcard); sem segredos no restore |
| T-02-06 | Elevation of Privilege | Bypass do fail-fast de JWT em ambientes não-Development | mitigate | Branch usa `builder.Environment.IsDevelopment()` (não apenas config flag) — qualquer ambiente não-Development (incluindo Production do compose) lança antes de servir requisições |

## Security Conventions (this plan)

- Comentários e mensagens de log em PT-BR (D-12); mensagem de erro do fail-fast cita explicitamente `JWT__KEY`
- `.env` nunca commitado (D-09) — verificar no `git status` final
- `postgres_data` é volume nomeado (não bind mount) — nada sensível no tree do repo
</threat_model>

<verification>
## Phase Checks (success criterion 1 do ROADMAP)

1. `docker compose up --build -d` sobe API + PostgreSQL com um comando e `docker compose ps` mostra ambos saudáveis
2. Fluxo curl completo contra `http://localhost:5290`: register 201 / duplicado 201 (anti-enumeração T-01-10) / login 200 / users 401 sem token / users 200 com token
3. Persistência comprovada após `docker compose restart api` (login 200)
4. Fail-fast comprovado: `docker compose config` falha sem `JWT__KEY`
5. Regressão local intacta: `dotnet test` 12 verdes + smoke local 401 (SQLite default)
</verification>

<success_criteria>
- `docker compose up --build` sobe API + PostgreSQL com um comando (ROADMAP SC-1)
- Postgres 16 com volume `postgres_data` persistente e healthcheck `pg_isready`
- API containerizada aplica migrações EF Core no startup com retry limitado; local usa `EnsureCreated` (D-02)
- Run local segue SQLite zero-dependência; 12 testes verdes
- Fail-fast de `JWT__KEY` ativo em produção (Program.cs + `${JWT__KEY:?}`)
- `.env.example` versionado com placeholders; `.env` gitignored
</success_criteria>

<output>
Create `.planning/phases/02-docker-docs-extra-p-s-mvp/02-01-SUMMARY.md` when done
</output>