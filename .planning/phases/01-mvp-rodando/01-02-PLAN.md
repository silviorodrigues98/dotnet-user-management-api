---
phase: 01-mvp-rodando
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/DotnetUserManagementApi.Api/wwwroot/index.html]
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, USER-01, QUAL-02]

must_haves:
  truths:
    - "Screen readers announce the tab state (role=tablist/role=tab + aria-selected toggling) and keyboard users see a visible focus ring"
    - "Form controls render in the same font as the rest of the page; placeholder text meets WCAG AA contrast (>= 4.5:1)"
    - "An empty users list shows 'Nenhum usuário cadastrado.' instead of a header-only table"
    - "Submit buttons disable while a request is in flight and re-enable after success or error (no double-submit)"
    - "The browser tab title reads 'Gerenciamento de Usuários' instead of the repo slug"
  artifacts:
    - path: "src/DotnetUserManagementApi.Api/wwwroot/index.html"
      provides: "Warning-level polish (a11y semantics, typography/contrast, copy, empty state, disabled state)"
      contains: ["role=\"tablist\"", "aria-selected", ":focus-visible", "font-family: inherit", "::placeholder", "Nenhum usuário cadastrado", "button:disabled"]
  key_links:
    - from: "switchTab()"
      to: "tab buttons"
      via: "aria-selected toggling"
      pattern: "setAttribute\\('aria-selected'"
    - from: "loadUsers()"
      to: "usersBody"
      via: "empty-state colspan row"
      pattern: "colspan"
    - from: "submitRegister()/submitLogin()"
      to: "form submit button"
      via: "disabled during in-flight request"
      pattern: "disabled = true"
---

<objective>
Address the 8 WARNING-level recommendations from 01-UI-REVIEW.md that fit naturally in the single-file frontend, WITHOUT touching the blocker fixes from plan 01 (same file — this plan runs in wave 2 after 01-01 completes).

Purpose: Lift the UI audit score beyond the 3 blockers by closing low-risk a11y, typography, contrast, and UX-feedback gaps. Three audit warnings are intentionally NOT addressed here (see Source Audit below): spacing scale drift (visual-regression risk for a "subtle" issue), color-only active-tab cue (design decision — luminance already differentiates per the audit), and design tokens (audit accepts them for a single-file app).

Output: A single modified file — `src/DotnetUserManagementApi.Api/wwwroot/index.html` — with the polish fixes grouped into 3 concern-based tasks.
</objective>

<execution_context>
@/home/silvi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/silvi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-rodando/01-UI-REVIEW.md
@.planning/phases/01-mvp-rodando/01-SECURITY.md
@src/DotnetUserManagementApi.Api/wwwroot/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab ARIA semantics and :focus-visible styling</name>
  <files>src/DotnetUserManagementApi.Api/wwwroot/index.html</files>
  <read_first>
    - src/DotnetUserManagementApi.Api/wwwroot/index.html — HTML lines ~31-43 (`.tabs`/`.tab` CSS), ~92-95 (tab buttons), ~97/106 (forms), CSS line ~54 (`input:focus { outline: none; ... }`), and `switchTab()` (lines ~142-147).
    - .planning/phases/01-mvp-rodando/01-UI-REVIEW.md — Pillar 2 "no tab semantics" and Pillar 6 "no focus-visible styling" warnings.
  </read_first>
  <action>
  HTML semantics: change `<div class="tabs">` to `<div class="tabs" role="tablist">`. On `#tabLogin` and `#tabRegister` add `role="tab"`, `aria-controls="loginForm"` / `aria-controls="registerForm"`, and initial `aria-selected="true"` (login) / `aria-selected="false"` (register). Add `role="tabpanel"` to `#loginForm` and `#registerForm`. JS: in `switchTab()` (after the existing classList toggles), also call `document.getElementById('tabLogin').setAttribute('aria-selected', tab === 'login')` and the equivalent for `tabRegister`. CSS: replace `input:focus { outline: none; border-color: #2563eb; }` with `input:focus { border-color: #2563eb; }`, and add `input:focus-visible, .tab:focus-visible, button:focus-visible { outline: 2px solid #60a5fa; outline-offset: 2px; }`. Remove the `outline: none` declaration entirely — the audit flags it for lacking a `:focus-visible` counterpart.
  </action>
  <acceptance_criteria>
    - index.html contains `role="tablist"`, two `role="tab"` buttons, `aria-selected` initial values, `aria-controls` on both tabs, and `role="tabpanel"` on both forms.
    - `switchTab()` calls `setAttribute('aria-selected', ...)` for both tabs.
    - A `:focus-visible` outline rule exists for input/tab/button.
    - `grep -c "outline: none"` on index.html returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'role="tablist"' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q 'aria-selected' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q 'setAttribute(.aria-selected' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q ':focus-visible' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& ! grep -q 'outline: none' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& echo PASS</automated>
    <live>dotnet run from src/DotnetUserManagementApi.Api; in the browser (or Playwright): Tab through the UI — a visible focus ring appears on tabs/buttons/inputs; with a screen-reader snapshot, the tablist/tab roles and aria-selected state are announced and toggle when switching tabs.</live>
  </verify>
  <done>Tab state is announced (role/aria-selected), panels are associated via aria-controls, and keyboard focus is always visible.</done>
</task>

<task type="auto">
  <name>Task 2: Fix form-control font inheritance and placeholder contrast</name>
  <files>src/DotnetUserManagementApi.Api/wwwroot/index.html</files>
  <read_first>
    - src/DotnetUserManagementApi.Api/wwwroot/index.html — CSS body rule (lines ~10-19, `font-family: system-ui, ...`), input styles (lines ~45-54), button styles (lines ~55-76).
    - .planning/phases/01-mvp-rodando/01-UI-REVIEW.md — Pillar 4 "form controls use a different font" (inputs/buttons compute to Arial) and Pillar 3 "placeholder contrast fails AA" (measured ~3.5:1, needs >= 4.5:1).
  </read_first>
  <action>
  Add one CSS rule right after the `body` rule: `input, button, textarea, select { font-family: inherit; }` so form controls inherit the body's system-ui stack instead of the UA default (Arial). Add one rule for placeholders: `::placeholder { color: #94a3b8; opacity: 1; }` — slate-400 has ~7:1 contrast on the `#0f172a` input background, comfortably passing AA 4.5:1; `opacity: 1` compensates for the Firefox placeholder-opacity quirk. Do not change any other color/font rules.
  </action>
  <acceptance_criteria>
    - index.html contains `font-family: inherit` scoped to form controls (input, button, textarea, select).
    - index.html contains a `::placeholder` rule setting `color: #94a3b8`.
    - Live: computed `font-family` of an input and a button equals the body's system-ui stack (not Arial); computed placeholder color is `rgb(148, 163, 184)`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "font-family: inherit" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q "::placeholder" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q "#94a3b8" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& echo PASS</automated>
    <live>dotnet run from src/DotnetUserManagementApi.Api; in the browser (or Playwright): evaluate `getComputedStyle(document.getElementById('loginEmail')).fontFamily` and `getComputedStyle(document.querySelector('button.primary')).fontFamily` — both contain "system-ui"; placeholder text is visibly lighter slate, contrast >= 4.5:1 (spot-check with a contrast tool).</live>
  </verify>
  <done>Form controls render in the page font; placeholder text meets WCAG AA.</done>
</task>

<task type="auto">
  <name>Task 3: Copy, empty state, and disabled submit state</name>
  <files>src/DotnetUserManagementApi.Api/wwwroot/index.html</files>
  <read_first>
    - src/DotnetUserManagementApi.Api/wwwroot/index.html — `<title>` (line ~6), `loadUsers()` (lines ~207-224, `tbody.innerHTML = ''` then loops `for (const user of body)`), `submitRegister()` (lines ~160-182) and `submitLogin()` (lines ~184-205).
    - .planning/phases/01-mvp-rodando/01-UI-REVIEW.md — Pillar 1 "title is the repo slug" and "missing empty state"; Pillar 6 "no disabled/submitting state" warnings.
  </read_first>
  <action>
  Title: replace `<title>dotnet-user-management-api</title>` with `<title>Gerenciamento de Usuários</title>`.
  Empty state: in `loadUsers()`, after `tbody.innerHTML = ''`, add `if (body.length === 0) { ... }` that creates one `<tr>` containing a single `<td>` with `colspan = 3`, `textContent = 'Nenhum usuário cadastrado.'`, and `style.textAlign = 'center'`. MUST use `document.createElement` + `textContent` — never innerHTML (preserves the T-01-15 XSS mitigation). Keep the existing per-user row loop unchanged.
  Disabled submit: in `submitRegister()` and `submitLogin()`, at the top of the function capture the submit button via `const btn = event.target.querySelector('button[type="submit"]')`, set `btn.disabled = true` before the `await request(...)`, and re-enable it in a `finally` block wrapping the existing try/catch (so it re-enables on both success and error paths). Add CSS rule `button:disabled { opacity: 0.6; cursor: not-allowed; }`.
  </action>
  <acceptance_criteria>
    - `<title>` is "Gerenciamento de Usuários".
    - `loadUsers()` contains an empty-array branch rendering a `colspan="3"` row with textContent "Nenhum usuário cadastrado." — and no innerHTML string is introduced anywhere in the file.
    - Both submit handlers set `btn.disabled = true` before the fetch and re-enable in `finally`.
    - CSS rule `button:disabled` exists.
    - Live: with a zero-user DB, the users table shows "Nenhum usuário cadastrado."; during register/login the submit button is disabled and re-enables after the response.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '<title>Gerenciamento de Usuários</title>' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q 'Nenhum usuário cadastrado' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q 'colspan' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q 'button:disabled' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q 'disabled = true' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& ! grep -q 'innerHTML' src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& echo PASS</automated>
    <live>dotnet run from src/DotnetUserManagementApi.Api; in the browser (or Playwright): (1) clear the DB (delete app.db) and load users after login — table shows "Nenhum usuário cadastrado."; (2) submit register — button disables immediately and re-enables on response; (3) browser tab shows "Gerenciamento de Usuários".</live>
  </verify>
  <done>Browser title is the product name; empty list communicates state; double-submit is prevented; all new DOM writes stay XSS-safe (textContent only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (browser) ↔ API | Server-provided user rows cross here — the empty-state branch adds a new DOM-write path |
| User input → DOM | Form submissions now gate on a disabled state — new control flow in submit handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Tampering (XSS) | `loadUsers()` empty-state row (index.html) | mitigate | Empty-state cell MUST be built with `document.createElement` + `textContent` — the plan's grep gate enforces `! grep -q 'innerHTML'` (extends T-01-15) |
| T-01-21 | Repudiation / Availability | Double-submit on register/login forms | mitigate | `btn.disabled = true` before the fetch + `finally` re-enable prevents duplicate POSTs (rate-limit T-01-11 stays the backend backstop) |
| T-01-22 | Spoofing — ARIA/markup manipulation | Tab semantics | accept | Role/aria attributes are assistive-tech hints, not security boundaries; no data crosses |
| T-01-23 | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — package legitimacy gate not applicable |

## Security Notes for Executor

- The `! grep -q 'innerHTML'` gate is a hard requirement — the file currently has zero innerHTML usages and must keep it that way (T-01-15).
- Do not touch the `request()` function or the blocker fixes from plan 01 (01-01) — same file, sequential wave.
</threat_model>

<verification>
Static gates (all must pass):
- `role="tablist"`, `aria-selected`, `setAttribute('aria-selected'`, `:focus-visible` present; `outline: none` absent (Task 1).
- `font-family: inherit`, `::placeholder`, `#94a3b8` present (Task 2).
- `<title>Gerenciamento de Usuários</title>`, "Nenhum usuário cadastrado", `colspan`, `button:disabled`, `disabled = true` present; `innerHTML` absent (Task 3).

Live re-test (executor runs app + Playwright):
1. Keyboard tab through UI — focus ring visible on tabs/buttons/inputs.
2. Zero-user DB after login — "Nenhum usuário cadastrado." row rendered.
3. Register/login submit — button disabled in-flight, re-enabled after response.
4. Browser tab title reads "Gerenciamento de Usuários".
5. Regression: re-run the 3 blocker checks from plan 01 (error message, success message, 375px scrollWidth) — must still pass.

## Source Audit (UI-REVIEW warnings coverage)

| Warning (UI-REVIEW) | Coverage |
|---------------------|----------|
| font-family mismatch on form controls | Plan 02 Task 2 |
| missing empty state for users table | Plan 02 Task 3 |
| no :focus-visible | Plan 02 Task 1 |
| tab aria semantics | Plan 02 Task 1 |
| placeholder contrast 3.5:1 (fails AA) | Plan 02 Task 2 |
| title tag is repo slug | Plan 02 Task 3 |
| no submit disabled state | Plan 02 Task 3 |
| spacing scale drift (0.55/0.85/0.9rem) | DEFERRED — audit calls it "subtle"; adjusting 5 spacing rules across one file risks visual regressions for negligible benefit; not a functional defect |
| color-only active-tab cue | DEFERRED — design decision; audit itself notes the luminance difference mitigates color-blind risk; changing tab affordance is a product call, not a defect fix |
| no design tokens (14 hardcoded hex) | DEFERRED — audit explicitly accepts tokens for a single-file app; tokenization adds churn with no consumer in a 250-line static file |
</verification>

<success_criteria>
- The 7 naturally-fitting warnings are closed; 3 are explicitly deferred with rationale (spacing drift, active-tab cue, design tokens).
- All blocker fixes from plan 01 remain intact (regression gate above).
- All changes are confined to `src/DotnetUserManagementApi.Api/wwwroot/index.html`.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-rodando/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Provides |
|----------|------|----------|
| `src/DotnetUserManagementApi.Api/wwwroot/index.html` (modified) | Source file | Task 1: tablist/tab/tabpanel roles + aria-selected toggling + :focus-visible outline; Task 2: `font-family: inherit` on form controls + `::placeholder` slate-400; Task 3: product title, empty-state colspan row, disabled-submit state |

Behavioral artifacts (observable in live app):
- Screen reader announces tab state; keyboard focus visible
- Form controls render in system-ui; placeholders meet AA contrast
- Zero-user DB shows "Nenhum usuário cadastrado."
- Register/login buttons disable during in-flight requests (no double-submit)
- Browser tab reads "Gerenciamento de Usuários"
