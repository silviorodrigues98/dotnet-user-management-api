---
phase: 01-mvp-rodando
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/DotnetUserManagementApi.Api/wwwroot/index.html]
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, AUTH-03, USER-01, QUAL-02]

must_haves:
  truths:
    - "Failed login (401) shows the API's real message 'E-mail ou senha inválidos.' instead of the generic 'Erro inesperado.'"
    - "After registering, the user sees the green success message 'Conta criada! Faça login para continuar.' rendered on the visible login tab"
    - "At 375px viewport the page has no horizontal scroll (scrollWidth <= 375); the logged-in email badge truncates with an ellipsis"
  artifacts:
    - path: "src/DotnetUserManagementApi.Api/wwwroot/index.html"
      provides: "All 3 BLOCKER fixes (error surfacing, register feedback, mobile layout)"
      contains: ["includes('json')", "text-overflow: ellipsis", "flex-wrap"]
  key_links:
    - from: "request()"
      to: "res.headers content-type"
      via: "content-type match"
      pattern: "includes\\('json'\\)"
    - from: "submitRegister()"
      to: "switchTab + loginMessage"
      via: "success message written after tab switch"
      pattern: "switchTab\\('login'\\)"
    - from: ".badge CSS"
      to: ".toolbar flex layout"
      via: "truncation + wrap"
      pattern: "text-overflow:\\s*ellipsis"
---

<objective>
Fix the 3 BLOCKER findings from 01-UI-REVIEW.md (gap source; UAT passed 10/10 with no gaps — these are visual/behavioral defects the UAT's DOM-level checks missed).

Purpose: Users currently get misleading feedback — every server error shows "Erro inesperado.", the only registration confirmation is invisible, and the layout breaks on phones. These are the top-priority fixes per the audit (overall 15/24).

Output: A single modified file — `src/DotnetUserManagementApi.Api/wwwroot/index.html` — with the 3 blocker fixes, each independently re-testable in the live app.
</objective>

<execution_context>
@/home/silvi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/silvi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-rodando/01-UI-REVIEW.md
@.planning/phases/01-mvp-rodando/01-UAT.md
@.planning/phases/01-mvp-rodando/01-SECURITY.md
@src/DotnetUserManagementApi.Api/wwwroot/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface real API error messages in request()</name>
  <files>src/DotnetUserManagementApi.Api/wwwroot/index.html</files>
  <read_first>
    - src/DotnetUserManagementApi.Api/wwwroot/index.html — the `request()` function (lines ~149-158): `const isJson = res.headers.get('content-type')?.includes('application/json');` followed by the `body?.detail || (body?.errors ? ...) || 'Erro inesperado.'` fallback chain.
    - .planning/phases/01-mvp-rodando/01-UI-REVIEW.md — Pillar 6 BLOCKER "all API error messages swallowed" and Pillar 1 generic-error finding.
    - .planning/phases/01-mvp-rodando/01-UAT.md — test 6 verified via curl that the 401 body carries `detail: "E-mail ou senha inválidos."` with Content-Type `application/problem+json`.
  </read_first>
  <action>
  In the `request()` function, change the content-type check on line ~151 from `includes('application/json')` to `includes('json')` so RFC 7807 responses (Content-Type `application/problem+json`) parse as JSON. Rationale: the API returns ProblemDetails for all 4xx/5xx; `application/problem+json` contains "json" as the subtype suffix, so `includes('json')` matches both `application/json` and `application/problem+json`. Do NOT change the existing fallback chain — it already surfaces `body.detail` (and flattens `body.errors` validation messages). Do NOT add logging or render any raw response body beyond detail/errors. Preserve the generic `'Erro inesperado.'` fallback for non-JSON error responses (masks 500s per T-01-07 — do not widen server-error disclosure).
  </action>
  <acceptance_criteria>
    - index.html contains exactly one `includes('json')` content-type check in `request()`.
    - index.html contains zero `includes('application/json')` checks.
    - Live: wrong-password login (401) renders "E-mail ou senha inválidos." in #loginMessage — not "Erro inesperado.".
  </acceptance_criteria>
  <verify>
    <automated>grep -c "includes('json')" src/DotnetUserManagementApi.Api/wwwroot/index.html | grep -q '^1$' && ! grep -q "includes('application/json')" src/DotnetUserManagementApi.Api/wwwroot/index.html && echo PASS</automated>
    <live>dotnet run from src/DotnetUserManagementApi.Api; in the browser (or Playwright), submit login with a wrong password — assert the visible #loginMessage text equals "E-mail ou senha inválidos." and does not contain "Erro inesperado."</live>
  </verify>
  <done>Login 401 shows the real API message; validation 400 messages surface; generic fallback retained only for non-JSON responses.</done>
</task>

<task type="auto">
  <name>Task 2: Render register success message on the login tab</name>
  <files>src/DotnetUserManagementApi.Api/wwwroot/index.html</files>
  <read_first>
    - src/DotnetUserManagementApi.Api/wwwroot/index.html — `submitRegister()` (lines ~160-182): success branch writes `message.className = 'message success'` + "Conta criada! Faça login para continuar." to `#registerMessage` and THEN calls `switchTab('login')`, which hides the register form — verified live: text in DOM, user sees nothing.
    - .planning/phases/01-mvp-rodando/01-UI-REVIEW.md — Pillar 1/Pillar 6 BLOCKER "success confirmation invisible" (lines 175-177).
  </read_first>
  <action>
  Reorder the success branch of `submitRegister()`: call `switchTab('login')` FIRST, then write the success message to the LOGIN form's element — `document.getElementById('loginMessage')` — with `className = 'message success'` and `textContent = 'Conta criada! Faça login para continuar.'`. Also reset `#registerMessage` (`className = 'message'`, `textContent = ''`) so stale text does not reappear when the user switches back to the register tab. Do NOT write the success message to `#registerMessage` after the tab switch — it must target `#loginMessage`, the element of the now-visible form. Leave the catch (error) branch writing to `#registerMessage` unchanged.
  </action>
  <acceptance_criteria>
    - In `submitRegister()`, the `switchTab('login')` call appears BEFORE the `loginMessage.className = 'message success'` assignment (ordering gate).
    - "Conta criada! Faça login para continuar." is assigned to `#loginMessage` (not `#registerMessage`).
    - Live: register a new user — the UI auto-switches to the login tab and shows a visible green "Conta criada! Faça login para continuar." paragraph under the login button.
  </acceptance_criteria>
  <verify>
    <automated>awk "/switchTab\\('login'\\)/{s=NR} /loginMessage\\.className = 'message success'/{l=NR} END{exit !(s>0 \&\& l>s)}" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& echo PASS; grep -q "Conta criada! Faça login para continuar." src/DotnetUserManagementApi.Api/wwwroot/index.html</automated>
    <live>dotnet run from src/DotnetUserManagementApi.Api; in the browser (or Playwright): fill register form (Nome, e-mail, senha >= 8 chars), submit — assert the login tab is active AND the visible paragraph reads "Conta criada! Faça login para continuar."</live>
  </verify>
  <done>After registration the user sees the green success confirmation on the login tab (visible, not display:none).</done>
</task>

<task type="auto">
  <name>Task 3: Eliminate mobile horizontal overflow (badge truncation)</name>
  <files>src/DotnetUserManagementApi.Api/wwwroot/index.html</files>
  <read_first>
    - src/DotnetUserManagementApi.Api/wwwroot/index.html — CSS lines ~84-85: `.toolbar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; }` and `.badge { background: #2563eb; ... font-size: 0.75rem; }` (no max-width/truncation).
    - .planning/phases/01-mvp-rodando/01-UI-REVIEW.md — Pillar 2/Pillar 5 WARNING: at 375px, email badge (222px, untruncated) overflows — scrollWidth 472 > 375.
  </read_first>
  <action>
  Modify the `.toolbar` rule (line ~84): add `gap: 0.5rem; flex-wrap: wrap; min-width: 0;` (keep `display:flex; justify-content:space-between; align-items:center; margin-bottom:1rem`). Modify the `.badge` rule (line ~85): add `max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` (keep existing background/color/padding/border-radius/font-size). Add a new rule `.toolbar h2 { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }` so the "Usuários cadastrados" heading can also truncate instead of forcing the toolbar wider than the card. Do NOT change other CSS.
  </action>
  <acceptance_criteria>
    - `.badge` rule contains `text-overflow: ellipsis`, `white-space: nowrap`, `overflow: hidden`, and `max-width: 100%`.
    - `.toolbar` rule contains `flex-wrap: wrap` and `min-width: 0`.
    - `.toolbar h2` truncation rule exists.
    - Live at 375px viewport after login: `document.documentElement.scrollWidth <= 375` (no horizontal scroll) and the long email badge renders truncated with an ellipsis inside the toolbar.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "text-overflow: ellipsis" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q "flex-wrap: wrap" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& grep -q "min-width: 0" src/DotnetUserManagementApi.Api/wwwroot/index.html \&\& echo PASS</automated>
    <live>dotnet run from src/DotnetUserManagementApi.Api; Playwright at 375x667: login with a long email (e.g. ui.audit.20260820@example.com), then evaluate `document.documentElement.scrollWidth <= window.innerWidth` — must be true; screenshot the toolbar — badge shows ellipsis.</live>
  </verify>
  <done>No horizontal scroll at 375px; the email badge truncates cleanly instead of overflowing the viewport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (browser) ↔ API | Untrusted HTTP responses cross here — this plan changes how error bodies (RFC 7807 ProblemDetails) are parsed and displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Information Disclosure | `request()` error rendering (index.html) | accept | `includes('json')` now surfaces `body.detail`/`body.errors` from the API — these are the exact domain messages the API already sends (T-01-07 mitigates 500s server-side with generic responses; the UI's "Erro inesperado." fallback remains for non-JSON bodies, so no disclosure widening) |
| T-01-18 | Tampering (XSS) | Error message + success message rendering | mitigate | All message writes use `textContent` (never innerHTML) — preserves the existing T-01-15 mitigation; executor must not introduce innerHTML in any modified branch |
| T-01-19 | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (single static-file edit) — package legitimacy gate not applicable |

## Security Notes for Executor

- Do NOT introduce `innerHTML` in the edited functions — T-01-15 (XSS mitigation) is enforced via `textContent` everywhere.
- Do NOT add client-side logging or rendering of raw HTTP bodies — only `detail`/`errors` fields, per T-01-07.
</threat_model>

<verification>
Static gates (all three must pass):
- `grep -c "includes('json')" index.html` == 1 and no `includes('application/json')` remains (Task 1).
- awk ordering gate: `switchTab('login')` line < `loginMessage.className = 'message success'` line (Task 2).
- `grep -q "text-overflow: ellipsis"` + `grep -q "flex-wrap: wrap"` + `grep -q "min-width: 0"` (Task 3).

Live re-test (executor runs app + Playwright, mirrors UAT methodology):
1. Start: `dotnet run --no-launch-profile` from `src/DotnetUserManagementApi.Api`.
2. Wrong-password login -> #loginMessage shows "E-mail ou senha inválidos." (BLOCKER 1 closed).
3. Register new user -> login tab visible with green "Conta criada! Faça login para continuar." (BLOCKER 2 closed).
4. Viewport 375x667 after login -> `scrollWidth <= 375`, badge truncated with ellipsis (BLOCKER 3 closed).
</verification>

<success_criteria>
- The 3 BLOCKER findings from 01-UI-REVIEW.md are resolved: real error messages surface, register success is visible, no mobile horizontal overflow.
- UAT flows still pass: register, login, users list, logout (no regressions).
- All changes are confined to `src/DotnetUserManagementApi.Api/wwwroot/index.html`.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-rodando/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Provides |
|----------|------|----------|
| `src/DotnetUserManagementApi.Api/wwwroot/index.html` (modified) | Source file | Blocker 1: `request()` parses `application/problem+json` and surfaces `body.detail`; Blocker 2: register success message rendered on the visible login tab; Blocker 3: `.toolbar` wrap/min-width + `.badge` ellipsis truncation |

Behavioral artifacts (observable in live app):
- 401 wrong-password login shows "E-mail ou senha inválidos." (was "Erro inesperado.")
- Post-registration green confirmation visible on the login tab
- 375px viewport: no horizontal scroll; email badge truncated with ellipsis
